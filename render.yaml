services:
  - type: web
    name: ocean-ac-dashboard
    runtime: node
    plan: free
    buildCommand: echo "No build required"
    startCommand: cd website && node server.js
    envVars:
      - key: NODE_ENV
        value: production
